# Raw capture frames — the built GIF in docs/ is what the README uses.
docs/frames/

node_modules/
.DS_Store
*.log

# A local checkout of bloub, kept for reference while porting the eye engine.
# Not part of the product; the credit lives in NOTICE.md.
reference/
