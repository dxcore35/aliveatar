# Token for the Cloudflare Tunnel that publishes this site at
# https://avatar.dxcore.store — get it from the Cloudflare dashboard under
# Zero Trust -> Networks -> Tunnels -> (your tunnel) -> "Install and run
# a connector" (it's the long string after "--token"). See DEPLOY.md.
CLOUDFLARE_TUNNEL_TOKEN=
